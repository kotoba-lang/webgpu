;; Task registry for `nbb scripts/run-task.cljs <task> [args…]` (ADR-2607173000).
;;
;; Originally auto-generated from bb.edn, which left this entry shelling straight back out to the
;; retired `bb` binary — an nbb registry that could not run on any machine without babashka
;; installed, i.e. on every murakumo fleet node. Converted for real in ADR-2608135000; the sibling
;; layout it needs now lives in deps.edn's :gen-compute-golden alias rather than in a classpath
;; string here, because that string had gone stale twice over without anything saying so.
{:gen-compute-golden
 {:cmd ["clojure" "-M:gen-compute-golden"]
  :doc "Regenerate fixtures/cartpole-compute-{step.wgsl,golden.json} from CLJC math + kami.physics-compute emit. Needs the siblings orgs/kotoba-lang/{kami-engine-sdk,cartpole-math} (declared as :local/root deps of the :gen-compute-golden alias). Assert no drift with `nbb scripts/run-task.cljs gen-compute-golden && git diff --exit-code`."}}
