;; Generated by bb_edn_to_nbb_tasks.cljs (ADR-2607173000).
;; Note: nbb does not resolve :deps from bb.edn; use sibling :paths or deps.edn+clojure.
{:paths ["src" "test" "../expr/src" "../wgsl/src" "../sprite-gpu/src" "../sprite2d/src" "../sky/src" "../shaders/src" "../render/src" "../render-shaders/src" "../scene2d/src" "../gpu/src" "../webgl/src" "../dance/src" "../physics/src" "../fsm/src" "../netsync/src" "../pipelines/src" "../level/src" "../host/src" "../playwright/src" "../cartpole-math/src"]
 :deps {
        ;; kotoba.pprint replaced clojure.pprint here (2026-09-24): the same
        ;; bytes (cljs.pprint transcribed). An explicit floor; tools.deps takes the
        ;; NEWEST sha it is shown.
        io.github.kotoba-lang/pprint {:git/sha "5c9d4aea71d6caac529fffcf361aba0f87ee485e"}
        ;; Declared HERE and not only in deps.edn: nbb reads nbb.edn and does NOT
        ;; read deps.edn or bb.edn, and the .cljs in this repo are run as
        ;; bare `nbb <file>`. A coordinate they cannot see is not a
        ;; declaration. sha and :paths are COPIED from deps.edn.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}}}
