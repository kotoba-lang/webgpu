# Rust Fixture Inventory

`kami-webgpu` is a CLJ/CLJS/CLJC repository. It has no `Cargo.toml` and no Rust
workspace. The only Rust file currently present is:

| file | role | authority |
|---|---|---|
| `fixtures/pipeline_specs.rs` | generated parity fixture | not authoritative |

`fixtures/pipeline_specs.rs` is generated by `bb gen-pipeline-specs` from
`kami.pipelines/native-pipelines`. It exists to prove that the EDN pipeline
descriptor can emit a native Rust-shaped fixture without changing the source of
truth.

## Policy

- Do not add a `Cargo.toml` to this repo.
- Do not hand-edit `fixtures/pipeline_specs.rs`.
- Native/Rust output must be generated from CLJC/EDN contracts.
- The authoritative WebGPU/render IR remains `src/kami/**/*.cljc` and related
  EDN data.

## Migration Status

The prior scan counted this repo as `0 Cargo.toml / 1 .rs`. That should be read
as `generated fixture`, not as a Rust dependency.

